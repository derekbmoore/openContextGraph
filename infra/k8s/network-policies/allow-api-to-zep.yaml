apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-api-to-zep
  namespace: ctxEco-prod
spec:
  podSelector:
    matchLabels:
      app: zep
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: ctxEco-backend
      ports:
        - port: 8000
          protocol: TCP
  policyTypes:
    - Ingress
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-api-to-zep
  namespace: ctxEco-prod
spec:
  podSelector:
    matchLabels:
      app: zep
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: backend
      ports:
        - protocol: TCP
          port: 8000

